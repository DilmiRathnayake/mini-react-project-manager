# mini-react-project-manager
A simple Project Management App built with React JS while practicing components, state, refs, and conditional rendering. Users can create projects with a title, description, and due date, add tasks under each project, and delete both projects and tasks.
